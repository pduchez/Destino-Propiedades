# Medición (opcional) — ver Medicion.astro y CLAUDE.md.
# Estas variables son PUBLIC (van al navegador): nunca poner aquí tokens,
# claves de API ni secretos de servidor.
# Si se deja vacío, no se carga ningún script de terceros (no hay tracking).

# Usar UNO de los dos (GTM tiene prioridad si ambos están definidos):
PUBLIC_GTM_CONTAINER_ID=
PUBLIC_GA4_MEASUREMENT_ID=
